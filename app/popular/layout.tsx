import { Metadata } from 'next';
import PopularTable from './page';

export const metadata: Metadata = {
  title: 'Popular coins | CryptoTracker',
  description: 'Check out the top 250 coins by market cap right now!',
};

export default PopularTable;
